% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/eda_artifact_generation.R
\name{generate_eda_artifacts}
\alias{generate_eda_artifacts}
\title{Generate EDA Artifacts}
\usage{
generate_eda_artifacts(
  data,
  DataName = NULL,
  UnivariateVars = NULL,
  CorrVars = NULL,
  TrendVars = NULL,
  TrendDateVar = NULL,
  TrendGroupVar = NULL,
  Theme = "dark",
  MaxCategoricalLevels = 25L,
  MaxDiscreteNumericLevels = 20L,
  CorrelationMethod = "spearman",
  MaxCorrelationPairsToPlot = 25L,
  HighCorrelationThreshold = 0.7,
  HistogramPlotCols = 2L,
  BoxPlotCols = 2L,
  GroupedBoxPlotCols = 2L,
  DiscreteNumericBarPlotCols = 2L,
  CategoricalBarPlotCols = 1L,
  OutputPath = NULL,
  ExportPNG = FALSE,
  ExportHTML = FALSE,
  PNGWidth = 1400,
  PNGHeight = 900,
  PNGDPI = 150,
  PNGBackground = "white",
  WebshotDelay = 0.2,
  IncludeDataURL = FALSE
)
}
\arguments{
\item{data}{A data.frame or data.table.}

\item{DataName}{Optional dataset name used in metadata.}

\item{UnivariateVars}{Character vector of variables for univariate analysis.
If `NULL`, all columns are considered.}

\item{CorrVars}{Character vector of variables for correlation analysis.}

\item{TrendVars}{Character vector of variables for trend analysis.}

\item{TrendDateVar}{Character scalar date variable used for trend plots.}

\item{TrendGroupVar}{Character scalar grouping variable used for grouped plots.}

\item{Theme}{Character scalar passed to AutoPlots plotting functions.}

\item{MaxCategoricalLevels}{Integer. Maximum categorical levels before an
"Other" bucket is created in categorical bar plots.}

\item{MaxDiscreteNumericLevels}{Integer. Maximum unique numeric values for a
numeric variable to be treated as discrete in bar plots.}

\item{CorrelationMethod}{Character scalar. Correlation method passed to
[stats::cor()] and [stats::cor.test()].}

\item{MaxCorrelationPairsToPlot}{Integer. Maximum correlation pair plots.}

\item{HighCorrelationThreshold}{Numeric. Absolute correlation threshold used
for high-correlation flags.}

\item{HistogramPlotCols, BoxPlotCols, GroupedBoxPlotCols, DiscreteNumericBarPlotCols, CategoricalBarPlotCols}{Integer layout hints for downstream RMarkdown display.}

\item{OutputPath}{Character scalar. Required when `ExportPNG` or `ExportHTML`
is `TRUE`. Sidecars are written under `images/` and `html/` subdirectories.}

\item{ExportPNG}{Logical. If `TRUE`, render display artifacts to PNG sidecars.}

\item{ExportHTML}{Logical. If `TRUE`, render display artifacts to HTML sidecars.}

\item{PNGWidth, PNGHeight}{Numeric. PNG export dimensions in pixels.}

\item{PNGDPI}{Numeric. PNG export resolution.}

\item{PNGBackground}{Character scalar. Background color used when rasterizing.}

\item{WebshotDelay}{Numeric. Delay in seconds before capturing htmlwidgets
with webshot2.}

\item{IncludeDataURL}{Logical. If `TRUE`, include Base64 data URLs in the
artifact tree. Defaults to `FALSE` to avoid bloating memory/RDS files.}
}
\value{
A named list with `metadata`, `qa`, `tables`, `widgets`, `plots`,
  `artifacts`, `layout`, and `llm_context`.
}
\description{
Generates tables, widgets, plots, QA outputs, and LLM-friendly context for an
EDA report. The function returns the original dynamic objects for RMarkdown
display and, optionally, sidecar PNG/HTML exports for LLM vision workflows or
portable reporting.
}
\details{
The legacy return structure is preserved through `tables`, `widgets`, and
`plots`. A wrapped artifact tree is also returned in `artifacts`, where each
display item has an `object`, optional `png`, optional `html`, and compact
metadata.
}
\examples{
\dontrun{
artifacts <- generate_eda_artifacts(
  data = data,
  DataName = "My Dataset",
  ExportPNG = TRUE,
  OutputPath = "eda_artifacts"
)

# Dynamic RMarkdown object:
artifacts$artifacts$plots$univariate$Histograms[[1]]$object

# PNG sidecar path:
artifacts$artifacts$plots$univariate$Histograms[[1]]$png
}

}
