% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/object_export_helpers.R
\name{ObjectToPNG}
\alias{ObjectToPNG}
\title{Save an Object as a PNG Image}
\usage{
ObjectToPNG(
  object,
  file,
  width = 1400,
  height = 900,
  dpi = 150,
  background = "white",
  overwrite = TRUE,
  selector = NULL,
  delay = 0.2,
  zoom = 1,
  selfcontained = FALSE,
  libdir = NULL,
  use_ragg = TRUE,
  ...
)
}
\arguments{
\item{object}{Plot object or plot-drawing function.}

\item{file}{Character scalar. Output PNG path.}

\item{width}{Numeric. Output width in pixels.}

\item{height}{Numeric. Output height in pixels.}

\item{dpi}{Numeric. Output resolution.}

\item{background}{Character scalar. Background color.}

\item{overwrite}{Logical. If `FALSE`, an existing file triggers an error.}

\item{selector}{Character scalar or `NULL`. Optional CSS selector passed to
[webshot2::webshot()] for HTML widgets.}

\item{delay}{Numeric. Delay in seconds before taking a webshot. Useful for
widgets that need a moment to render.}

\item{zoom}{Numeric. Zoom factor passed to [webshot2::webshot()].}

\item{selfcontained}{Logical. Passed to [htmlwidgets::saveWidget()] for
HTML widgets. Defaults to `FALSE` so widget screenshot staging does not
require pandoc.}

\item{libdir}{Character scalar or `NULL`. Dependency directory passed to
[htmlwidgets::saveWidget()] when `selfcontained = FALSE`.}

\item{use_ragg}{Logical. If `TRUE` and \pkg{ragg} is installed, uses
[ragg::agg_png()] for base graphics and recorded plots.}

\item{...}{Additional arguments passed to lower-level methods.}
}
\value{
Invisibly returns the normalized PNG file path.
}
\description{
Converts a supported visualization object to a PNG file.
}
\details{
This helper is designed for artifact-first reporting workflows where plots
are generated once and then reused in RMarkdown, HTML reports, PDFs, or
LLM vision prompts.

Supported object types:

\itemize{
  \item \pkg{ggplot2} objects.
  \item HTML widgets, including \pkg{echarts4r}, \pkg{plotly}, and other
    \pkg{htmlwidgets}.
  \item `recordedplot` objects from base R graphics.
  \item Functions that draw a plot when called.
}

HTML widgets are saved to a temporary HTML file and rendered to PNG using
\pkg{webshot2}. Base graphics are drawn into a PNG device.
}
\examples{
\dontrun{
p <- ggplot2::ggplot(mtcars, ggplot2::aes(wt, mpg)) +
  ggplot2::geom_point()

ObjectToPNG(
  object = p,
  file = "scatter.png",
  width = 1400,
  height = 900
)
}

}
